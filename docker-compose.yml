services:
  myapi:
    image: myapi:latest
    container_name: myapi-v1.0.1
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TIMEOUT=8000
    volumes:
      - myapi-logs:/app/logs
    networks:
      - myapi-network

volumes:
  myapi-logs:
    driver: local

networks:
  myapi-network:
    driver: bridge
